apiVersion: v1
kind: Service
metadata:
  name: guestbook-ui
spec:
  ports:
  - port: 80
    targetPort: 80
  selector:
    app: guestbook-ui
---
apiVersion: v1
kind: Service
metadata:
  name: guestbook-ui-nodeport
spec:
  ports:
  - port: 30180
    targetPort: 80
  selector:
    app: guestbook-ui
  type: NodePort